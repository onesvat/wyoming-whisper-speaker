name: Build and Push Docker Images

on:
  push:
    branches:
      - main
    tags:
      - 'v*'
  pull_request:
    branches:
      - main
  workflow_dispatch:

env:
  DOCKER_HUB_REPO: onesvat/wyoming-whisper-speaker

jobs:
  prepare:
    runs-on: ubuntu-latest
    outputs:
      version: ${{ steps.version.outputs.version }}
      is_tag: ${{ steps.version.outputs.is_tag }}
    steps:
      - name: Determine version
        id: version
        run: |
          if [[ "${{ github.ref }}" == refs/tags/v* ]]; then
            VERSION="${GITHUB_REF#refs/tags/v}"
            IS_TAG="true"
            echo "version=${VERSION}" >> $GITHUB_OUTPUT
            echo "is_tag=${IS_TAG}" >> $GITHUB_OUTPUT
            echo "Building version: ${VERSION} (from git tag)"
          else
            VERSION=""
            IS_TAG="false"
            echo "version=${VERSION}" >> $GITHUB_OUTPUT
            echo "is_tag=${IS_TAG}" >> $GITHUB_OUTPUT
            echo "Building from branch: ${GITHUB_REF#refs/heads/}"
          fi

  build-and-push:
    needs: prepare
    runs-on: ubuntu-latest
    strategy:
      matrix:
        variant:
          - { dockerfile: 'Dockerfile.gpu', tag: 'gpu' }
          - { dockerfile: 'Dockerfile.cpu', tag: 'cpu' }

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to Docker Hub
        if: github.event_name != 'pull_request'
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_HUB_USERNAME }}
          password: ${{ secrets.DOCKER_HUB_TOKEN }}

      - name: Generate Docker tags
        id: tags
        run: |
          VARIANT="${{ matrix.variant.tag }}"
          VERSION="${{ needs.prepare.outputs.version }}"
          IS_TAG="${{ needs.prepare.outputs.is_tag }}"
          REPO="${{ env.DOCKER_HUB_REPO }}"
          
          if [[ "$IS_TAG" == "true" ]]; then
            MAJOR_MINOR=$(echo "$VERSION" | cut -d. -f1,2)
            TAGS="${REPO}:${VARIANT}-v${VERSION},${REPO}:${VARIANT}-${MAJOR_MINOR},${REPO}:${VARIANT}"
            
            if [[ "$VARIANT" == "gpu" ]]; then
              TAGS="${TAGS},${REPO}:v${VERSION},${REPO}:${VERSION},${REPO}:latest"
            fi
            
            echo "Generated tags (version $VERSION):"
            echo "$TAGS" | tr ',' '\n'
          else
            BRANCH="${GITHUB_REF#refs/heads/}"
            TAGS="${REPO}:${VARIANT}"
            
            if [[ "$VARIANT" == "gpu" && "$BRANCH" == "main" ]]; then
              TAGS="${TAGS},${REPO}:latest"
            fi
            
            echo "Generated tags (branch $BRANCH):"
            echo "$TAGS" | tr ',' '\n'
          fi
          
          echo "tags=${TAGS}" >> $GITHUB_OUTPUT

      - name: Build and push Docker image
        uses: docker/build-push-action@v5
        with:
          context: .
          file: ${{ matrix.variant.dockerfile }}
          platforms: linux/amd64,linux/arm64
          push: ${{ github.event_name != 'pull_request' }}
          tags: ${{ steps.tags.outputs.tags }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

  notify:
    needs: [prepare, build-and-push]
    runs-on: ubuntu-latest
    if: github.event_name != 'pull_request' && success()
    steps:
      - name: Notify on success
        run: |
          VERSION="${{ needs.prepare.outputs.version }}"
          IS_TAG="${{ needs.prepare.outputs.is_tag }}"
          
          if [[ "$IS_TAG" == "true" ]]; then
            echo "✅ Docker images v${VERSION} successfully built and pushed"
            echo "Version tags:"
            echo "  - gpu-v${VERSION}, cpu-v${VERSION}"
            echo "  - v${VERSION}, ${VERSION}"
          else
            echo "✅ Docker images successfully built and pushed from branch"
            echo "Branch tags:"
            echo "  - gpu, cpu, latest"
          fi